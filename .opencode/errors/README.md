# Errors log — mike-website

Per-project memory of mistakes made during agent sessions, so they never repeat.
One file per topic; index them here as they're created.

## Index

_(empty — no entries yet)_

## Entry format

`<topic>.md` with: date, what went wrong, root cause, the rule that prevents it.
The coder hands entries verbatim; doc-keeper may append and index but never
invents or rewords them.
