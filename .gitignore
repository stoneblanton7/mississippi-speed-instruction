# Environment variables — never commit secrets
.env
.env.local
.env.*.local
*.env

# Dependencies
node_modules/
dist/
build/

# Production build zips for Sam's prod handoff (regenerate via npm run build)
msi-prod-build*.zip

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Firecrawl raw outputs (large, can be regenerated)
# (kept — scrape-output/ IS committed per workflow)
